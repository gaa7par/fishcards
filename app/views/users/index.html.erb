<h1>Fishcards</h1>
<h2>Users</h2>

<ul>
  <% @users.each do |user| %>
  <li>
    <%= user.email %>
    <%= link_to 'Show', user_path(user) %>
  </li>
  <% end %>
</ul>

<%= link_to 'New user', new_user_path(@user) %>
